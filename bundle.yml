schema_version: "1.0"
bundle:
  id: sdd-cycle
  name: "SDD Cycle"
  version: "1.2.0"
  role: "workflow"
  description: "Local-first SDD chain: branch→specify→plan→tasks→analyze→apply→gate→implement, with a mandatory feature branch first and a one-command launcher"
  author: "peterelmwood"
  license: "MIT"
requires:
  speckit_version: ">=0.8.5"
provides:
  extensions:
    - id: sdd
      version: "1.0.0"
  workflows:
    - id: sdd
      version: "1.2.0"
